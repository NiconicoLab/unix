~/.bash_profile
source .bashrc
#→~/.bashrcのファイルの読み込み

————————————————————————————————————————————————————————————————————————————————————

~/.bashrc

to_trash() {
    for file in $@
    do
        mv $file ~/.trash_
    done
}
#→rmでゴミ箱に移動するように設定

